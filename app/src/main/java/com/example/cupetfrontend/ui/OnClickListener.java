package com.example.cupetfrontend.ui;

/**
 * An interface defining a class responsible for listening to
 * when some view is clicked.
 */
public interface OnClickListener {
    /**
     * A method that's called when the view is clicked
     */
    void onClick();
}
